\documentclass[border=8pt]{article}
\usepackage{tikz}
\usepackage{geometry}
\geometry{margin=0.2cm, paperwidth=20cm, paperheight=8cm}
\usetikzlibrary{positioning}
\usetikzlibrary{shapes.geometric}
\usetikzlibrary{shadows}

% Define colors
\definecolor{frozenblue}{RGB}{100,150,200}
\definecolor{trainableyellow}{RGB}{255,200,100}
\definecolor{replaygreen}{RGB}{100,200,100}
\definecolor{outputpurple}{RGB}{200,100,200}

% Define Box style (3D box)
\tikzset{
    Box/.pic={
        \tikzset{/boxblock/.cd,#1}
        \tikzstyle{box}=[every edge/.append style={pic actions, densely dashed, opacity=.7},fill opacity=\opacity, pic actions,fill=\fill]
        
        \pgfmathsetmacro{\y}{\cubey*\scale}
        \pgfmathsetmacro{\z}{\cubez*\scale}
        \pgfmathsetmacro{\x}{\cubex*\scale}
        
        % Draw 3D box
        \coordinate (a) at (0, \y/2, \z/2);
        \coordinate (b) at (0,-\y/2, \z/2);
        \coordinate (c) at (\x,-\y/2, \z/2);
        \coordinate (d) at (\x, \y/2, \z/2);
        \coordinate (e) at (\x, \y/2,-\z/2);
        \coordinate (f) at (\x,-\y/2,-\z/2);
        \coordinate (g) at (0,-\y/2,-\z/2);
        \coordinate (h) at (0, \y/2,-\z/2);
        
        % Draw faces
        \draw [box] (d) -- (a) -- (b) -- (c) -- cycle; % front
        \draw [box] (d) -- (e) -- (f) -- (c) -- cycle; % right
        \draw [box] (d) -- (a) -- (h) -- (e) -- cycle; % top
        \draw [box] (b) -- (g) -- (f) -- (c) -- cycle; % bottom
        \draw [box] (a) -- (h) -- (g) -- (b) -- cycle; % left
        \draw [box] (h) -- (e) -- (f) -- (g) -- cycle; % back
        
        % Captions added manually after replay buffer
        
        % Define connection points
        \coordinate (\name-west) at (0,0,0);
        \coordinate (\name-east) at (\x,0,0);
        \coordinate (\name-north) at (\x/2,\y/2,0);
        \coordinate (\name-south) at (\x/2,-\y/2,0);
        \coordinate (\name-center) at (\x/2,0,0);
    },
    /boxblock/.search also={/tikz},
    /boxblock/.cd,
    width/.store in=\cubex,
    height/.store in=\cubey,
    depth/.store in=\cubez,
    scale/.store in=\scale,
    caption/.store in=\caption,
    name/.store in=\name,
    fill/.store in=\fill,
    opacity/.store in=\opacity,
    captionoffset/.store in=\captionoffset,
    fill={rgb:red,5;green,5;blue,5;white,15},
    opacity=0.8,
    width=2,
    height=13,
    depth=15,
    scale=.2,
    caption=,
    name=,
    captionoffset=1.2,
}

\begin{document}
\begin{tikzpicture}[scale=1.0]

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% Forward Pass (Top Section)
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

% Title
\node[font=\large\bfseries] at (0, 6) {Forward Pass};

% Input (proportional to 12×100)
\pic[shift={(0,0,0)}] at (0,4.5) {Box={name=input,%
        caption={},%
        fill=gray!20,opacity=0.8,width=1.2,height=10,depth=8,scale=0.2}};

% Frozen Layers Section (much closer together, proportional)
\pic[shift={(0,0,0)}] at (1.8,4.5) {Box={name=conv1,%
        caption={},%
        fill=frozenblue,opacity=0.8,width=1.6,height=8,depth=8,scale=0.2}};

\pic[shift={(0,0,0)}] at (3.6,4.5) {Box={name=conv2,%
        caption={},%
        fill=frozenblue,opacity=0.8,width=3.2,height=8,depth=8,scale=0.2}};

\pic[shift={(0,0,0)}] at (6.0,4.5) {Box={name=gap,%
        caption={},%
        fill=frozenblue,opacity=0.8,width=1.0,height=8,depth=8,scale=0.2}};

\pic[shift={(0,0,0)}] at (7.2,4.5) {Box={name=features,%
        caption={},%
        fill=frozenblue,opacity=0.8,width=3.2,height=8,depth=8,scale=0.2}};

% Frozen layers bracket and arrow removed for cleaner look

% Trainable Layers Section (much closer together, proportional)
\pic[shift={(0,0,0)}] at (10.8,4.5) {Box={name=head,%
        caption={},%
        fill=trainableyellow,opacity=0.8,width=3.2,height=8,depth=8,scale=0.2}};

\pic[shift={(0,0,0)}] at (14.4,4.5) {Box={name=output,%
        caption={},%
        fill=outputpurple,opacity=0.8,width=7.0,height=8,depth=8,scale=0.2}};

% Trainable layers bracket and arrow removed for cleaner look

% Forward arrows with dimension labels
\draw[->, thick] (input-east) -- (conv1-west);
\node[font=\small, color=black] at (0.9, 5.2) {12×100};

\draw[->, thick] (conv1-east) -- (conv2-west);
\node[font=\small, color=black] at (2.7, 5.2) {12→16};

\draw[->, thick] (conv2-east) -- (gap-west);
\node[font=\small, color=black] at (4.8, 5.2) {16→32};

\draw[->, thick] (gap-east) -- (features-west);
\node[font=\small, color=black] at (6.6, 5.2) {32-dim};

\draw[->, thick] (features-east) -- (head-west);
\node[font=\small, color=black] at (9.0, 5.2) {32→7};

\draw[->, thick] (head-east) -- (output-west);
\node[font=\small, color=black] at (12.6, 5.2) {7 classes};

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% Replay Buffer (Side Section)
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

% Replay Buffer (3D disk, smaller, bottom left)
\pic[shift={(0,0,0)}] at (2,1.5) {Box={name=replay,%
        caption={Replay\\Buffer},%
        fill=replaygreen,opacity=0.8,width=1.5,height=8,depth=8,scale=0.2}};

% Replay connections (to bottom left)
\draw[<->, thick, green!70!black] (features-south) -- (replay-north);
\draw[<->, thick, green!70!black] (head-south) -- (replay-north);

% Replay labels
\node[font=\tiny, color=green!70!black] at (8.2, 3.2) {Store};
\node[font=\tiny, color=green!70!black] at (10.8, 3.2) {Retrieve};

% INT8 transformation removed - keeping it simple

% Add layer names just above the boxes (larger text)
\node[font=\small, color=black] at (0, 3.2) {Input};
\node[font=\small, color=black] at (1.8, 3.2) {Conv1D};
\node[font=\small, color=black] at (3.6, 3.2) {Conv1D};
\node[font=\small, color=black] at (6.0, 5.7) {GAP};
\node[font=\small, color=black] at (7.5, 5.7) {Features};
\node[font=\small, color=black] at (10.8, 5.6) {TDM Head};
\node[font=\small, color=black] at (14.4, 5.6) {Softmax};

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% Backward Pass (Bottom Section)
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

% Title (removed - gradients are shown by arrows above)

% Backward arrows (only for trainable layers)
% \draw[<-, thick, purple!70!black] (head-south) -- ++(0, -0.8);
% \draw[<-, thick, purple!70!black] (output-south) -- ++(0, -0.8);

% Gradient labels removed - arrows are self-explanatory

% Frozen layers note removed for cleaner look

% Legend removed - colors are self-explanatory

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% Precision Constraint
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

% Precision constraint removed for cleaner look

\end{tikzpicture}
\end{document}
